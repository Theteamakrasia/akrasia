# Database Schema

Place your SQL / schema files here once the database is chosen.

## Naming Convention
- `001_create_users.sql`
- `002_create_products.sql`

Run migrations in order. See `../migrations/` for migration scripts.
