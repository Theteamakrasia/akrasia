/**
 * Root router — mount all sub-routers here.
 *
 * Example (Express):
 *   const router = require("express").Router();
 *   router.use("/auth",    require("./auth"));
 *   router.use("/users",   require("./users"));
 *   router.use("/contact", require("./contact"));
 *   module.exports = router;
 */
