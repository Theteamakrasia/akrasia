/**
 * Environment config
 * Replace values per environment (dev / staging / prod)
 */
const ENV = {
  API_BASE_URL: "https://akrasia-production.up.railway.app/api",   // update for prod
  APP_NAME:     "Akrasia",
  VERSION:      "1.0.0",
};

export default ENV;
